# 树通 Flutter — 更新日志 v1.0.1

> 与 v0.5.1 更新日志同格式的推送内容，直接录入后端版本 API（`/node/versions`）即可在版本页展示。
> 更新范围：`f9513a7`（2026-07-25）→ `fbdb233`（2026-08-17）。

## 推送给客户端的记录（JSON，字段与 `GET /versions/latest` 返回结构一致）

```json
{
  "id": 3,
  "versionNumber": "1.0.1",
  "platform": "android",
  "title": "界面现代化与搜索重构",
  "log": "新增：底部导航与发布入口、广场品类顶栏、左侧下拉刷新球、搜索页四维筛选与历史管理、个人资料页、时区选择、设置页重组\n优化：帖子卡片与评论输入对齐、图片查看器、状态栏配色统一、主题参数化",
  "description": "一，新增功能：\n1.底部导航重构：抽屉改为「广场/收藏/消息/我的」四 Tab + 中间发布按钮，发布成功自动回广场默认分类并刷新\n2.广场顶栏品类 Tab（默认/问答/资料/兴趣/梗图），品类走后端过滤，顶栏样式参数化\n3.左侧下拉刷新球：列表置顶任意位置下拉唤出，左上角滑入，不干扰顶栏点击与列表滚动\n4.搜索页重构：关键词/作者/时间段/品类四维筛选，排序/时间筛选面板，自定义日期范围选择器（中文）\n5.搜索历史：长按单条删除、清空二次确认、搜索后自动隐藏\n6.个人资料详情页（昵称/签名编辑），用户页资料卡 + 分组栏目整体居中\n7.时区服务：内置 14 个固定偏移时区，独立时区设置页，帖子时间统一按选定时区显示\n8.设置页重组：通知/隐私政策/系统/时区设置页，Android 更新检查入口，破坏性操作统一确认对话框\n9.未登录时用户页显示注册引导，点击跳转注册，返回自动刷新\n二，优化修复：\n1.图片查看器拦截返回键先关预览、cover 填充消除边缘黑框\n2.评论输入栏对齐帖子最底部（回复区/日期行），回复按钮与菜单颜色统一，点击区域扩大\n3.移除缩略图缓存尺寸参数，消除列表小图变形\n4.状态栏颜色与页面顶部元素背景统一（广场/搜索/用户/设置）\n5.主题参数化：底部导航/广场顶栏/刷新球/搜索页四个主题文件，背景统一纯白/#191919\n6.搜索排序改为客户端基于 idListv2 元数据排序，规避后端 sort 不支持；接口错误兜底\n7.发帖页、注册页、版本页交互回归精简版式，保留单张删除/触感等新能力\n8.分类切换 loading 死锁、下拉刷新手势冲突等修复",
  "downloadUrl": "https://www.leisure.xin:33433/flutter_app_version/v1.0.1/",
  "releaseDate": "2026-08-17T00:00:00Z"
}
```

## 版本页展示效果

**v1.0.1 · 界面现代化与搜索重构**

> 新增：底部导航与发布入口、广场品类顶栏、左侧下拉刷新球、搜索页四维筛选与历史管理、个人资料页、时区选择、设置页重组  
> 优化：帖子卡片与评论输入对齐、图片查看器、状态栏配色统一、主题参数化

**一，新增功能：**
1. 底部导航重构：抽屉改为「广场/收藏/消息/我的」四 Tab + 中间发布按钮，发布成功自动回广场默认分类并刷新
2. 广场顶栏品类 Tab（默认/问答/资料/兴趣/梗图），品类走后端过滤，顶栏样式参数化
3. 左侧下拉刷新球：列表置顶任意位置下拉唤出，左上角滑入，不干扰顶栏点击与列表滚动
4. 搜索页重构：关键词/作者/时间段/品类四维筛选，排序/时间筛选面板，自定义日期范围选择器（中文）
5. 搜索历史：长按单条删除、清空二次确认、搜索后自动隐藏
6. 个人资料详情页（昵称/签名编辑），用户页资料卡 + 分组栏目整体居中
7. 时区服务：内置 14 个固定偏移时区，独立时区设置页，帖子时间统一按选定时区显示
8. 设置页重组：通知/隐私政策/系统/时区设置页，Android 更新检查入口，破坏性操作统一确认对话框
9. 未登录时用户页显示注册引导，点击跳转注册，返回自动刷新

**二，优化修复：**
1. 图片查看器拦截返回键先关预览、cover 填充消除边缘黑框
2. 评论输入栏对齐帖子最底部（回复区/日期行），回复按钮与菜单颜色统一，点击区域扩大
3. 移除缩略图缓存尺寸参数，消除列表小图变形
4. 状态栏颜色与页面顶部元素背景统一（广场/搜索/用户/设置）
5. 主题参数化：底部导航/广场顶栏/刷新球/搜索页四个主题文件，背景统一纯白/#191919
6. 搜索排序改为客户端基于 idListv2 元数据排序，规避后端 sort 不支持；接口错误兜底
7. 发帖页、注册页、版本页交互回归精简版式，保留单张删除/触感等新能力
8. 分类切换 loading 死锁、下拉刷新手势冲突等修复

---

## 附注

- `id`、`releaseDate`、`downloadUrl` 按后端实际填写即可，此处为占位值。
- 客户端版本号已同步更新：`pubspec.yaml` → `1.0.1+3`，`lib/models/version_info.dart` 的 `currentVersion` → `'1.0.1'`（版本详情页据此标记「当前版本」）。
