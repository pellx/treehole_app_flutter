---
description: Flutter/Dart 编码最佳实践（改编自 awesome-cursorrules Flutter Expert）
globs: "**/*.dart"
alwaysApply: false
---

# Flutter / Dart 最佳实践

> 来源：PatrickJS/awesome-cursorrules（40k+ stars）Flutter App Expert 规则，
> 已按本项目实际架构裁剪 —— 本项目不使用 BLoC / GetIt / GoRouter，
> 采用 StatefulWidget + services 单例/静态类，勿引入新状态管理框架。

## 通用
- 遵循现有项目架构，不强行套用其他项目结构
- 严格空安全；不写 `!` 强解包除非逻辑上必然非空
- 错误处理：捕获后 `debugPrint` + 返回 null / 错误标记，禁止空 catch 吞异常
- 命名遵循 Effective Dart：类 UpperCamelCase、成员 lowerCamelCase、文件 snake_case

## Widget
- Widget 保持小而聚焦，超长 build 拆分为私有方法或独立 Widget
- 尽可能使用 `const` 构造
- 列表用 ListView.builder 等惰性构建，避免一次性构建全部子项
- 正确处理生命周期：`dispose` 中释放 Controller / Timer / FocusNode

## 性能
- 图片使用缓存（本项目：缩略图存 Hive，先查 `PostStorage.getThumbnail`）
- 批量网络请求用 `Future.wait` 并行，避免 for + await 串行
- 高频 `setState` 前先比较值是否变化，减少无效重建
- 重计算（如 PoW、hash）放 `Isolate.run`，不阻塞 UI 线程

## 测试
- 业务逻辑（hash 计算、数据解析）优先补单元测试
- 修改 `session_service.dart` 的 hash 算法后必须与后端对照验证
