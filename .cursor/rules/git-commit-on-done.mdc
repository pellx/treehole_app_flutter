---
description: 每次对话完成时必须提交 git（用户强制约定）
alwaysApply: true
---

# 对话结束必须提交 Git

用户约定：**每一轮对话在任务完成后，Agent 必须主动创建 git commit**，无需再询问是否提交。

## 何时提交

- 本轮已产生或保留有意义的工作区改动（代码、规则、配置等）时，在回复用户收尾前完成提交
- 纯问答、未改文件则不提交，不造空 commit
- 用户明确说「先别提交 / 不要提交」时，当轮遵从用户，下轮默认恢复本规则

## 如何提交

1. 并行查看：`git status`、`git diff`（含 staged/unstaged）、`git log`（对齐近期 message 风格）
2. 只暂存本轮相关文件；排除密钥、`.env`、明显临时目录（如 `tmp_*`、`tmp_backend_patch/`、`.hermes/`）
3. 用 HEREDOC 写简洁 commit message（中文或仓库既有风格均可），说明「为什么」而非堆文件列表
4. `git add` → `git commit` → `git status` 确认成功
5. **不要** `git push`，除非用户明确要求
6. 遵守既有 Git 安全约定：不改 config、不用 `-i`、不 force push、不随意 amend（amend 仅在用户要求且满足安全条件时）

## 与其它规则的关系

- 本规则优先于「不要主动 git commit」类旧表述
- 提交失败（含 hook 拒绝）时：修复后重新 **新建** commit，不要强行 amend
