---
description: Agent 行为约束：多模型分工流水线、节省 token、子代理规范
alwaysApply: true
---

# Agent 行为规则

## 多模型分工（按阶段选用）

复杂任务按流水线协作；简单单步任务由当前模型直接完成，不必硬套全流程。

| 阶段 | 模型 | 职责 |
|------|------|------|
| 1. 需求对接 | `Cursor Grok 4.5` | 与用户对齐需求、澄清边界、指定可执行方案（目标/范围/验收标准/步骤） |
| 2. 网络检索 | `Composer 2.5` | 查官方文档、API 变更、第三方方案、最新实践；产出可引用结论与链接 |
| 3. 本地理解 | `Composer 2.5` | 读仓库规则与代码、定位相关文件、归纳现状与约束；不做大范围改码 |
| 4. 代码编写 | `Cursor Grok 4.5` | 按已定方案最小 diff 改代码；遵守项目分层与既有约定 |
| 5. 代码审计 | `Composer 2.5` | 审查 diff：正确性、边界、安全、约定合规；只报问题与改法，不擅自扩 scope |

### 交接要求

- 上一阶段交付物须精炼（结论 + 关键路径/链接 + 待决问题），供下一阶段直接使用
- 方案未定前禁止写码；写码未完成前不做最终审计结论
- 审计发现问题：回到 `Cursor Grok 4.5` 修复，再由 `Composer 2.5` 复审相关 diff

## 节省 Token

- 回答直击要点，先给结论；不复述用户已知信息，不罗列不会采用的备选方案
- 读文件优先用精确搜索（Grep/Glob）定位后按行读取，不要整读大文件；已读过的文件不重复读取
- 不输出大段未改动的代码；引用现有代码用行号引用，不粘贴全文
- 编辑用最小 diff（StrReplace），不用整文件重写；禁止生成冗长的叙述性注释
- 简单问题不启动子代理、不建 todo list；工具调用能并行就并行，减少往返轮次
- 总结控制在必要长度，不使用装饰性表格/标题堆砌