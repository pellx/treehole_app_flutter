# 后端接口修改需求：搜索页

> 对应前端需求：关键词搜索、指定作者搜索、指定时间段搜索、指定品类搜索。

## 一、现状

当前搜索页只接入了 `GET /posts/idList` 的 `search` 参数（ES 全文检索）。

根据 `API.md` 描述，`/posts/idList` 已经支持：

- `search`：关键词全文检索
- `author`：精确匹配作者
- `dateStart`、`dateEnd`：按 `created_at` 日期范围过滤

但前端目前只传了 `search`。

## 二、本次需要新增/调整的内容

### 1. 帖子新增 `category` 字段

目前帖子表/ES 中没有品类字段，广场页和搜索页的品类都是前端用关键词硬猜的，不准确。

建议：

- 帖子表新增 `category` 字段，枚举类型，建议 key 与前端一致：
  - `问答`
  - `资料`
  - `兴趣`
  - `梗图`
  - （可选默认值，如 `默认` / `other`）
- 后端提供枚举常量，返回给前端时也用这些中文 key 或对应英文 key（需前后端约定）。
- 历史数据：
  - 方案 A：给所有历史帖子打默认品类 `默认`。
  - 方案 B：用已有内容关键词自动回填一次（推荐作为一次性脚本）。

### 2. `POST /posts/create` 支持传入 `category`

发布帖子时允许客户端带上 `category`。

示例请求体：

```json
{
  "title": "标题",
  "content": "正文",
  "is_anonymous": false,
  "category": "问答",
  "images": [],
  "attachments": []
}
```

- 字段可选：不传时后端使用默认值。
- 校验：若传了，必须是允许的枚举值之一。

### 3. `GET /posts/idList` 新增 `category` 查询参数

在已有 `search`、`author`、`dateStart`、`dateEnd` 基础上，新增：

- `category`：按品类精确匹配，支持单值，如 `category=问答`。

组合逻辑：

- 所有参数 AND 组合。
- `category` 为空或不传时，不限制品类。

示例请求：

```
GET /posts/idList?search=Flutter&author=alice&category=问答&dateStart=2025-08-01&dateEnd=2025-08-14
```

### 4. Elasticsearch 索引调整

如果 `search` 参数走 ES，需要：

- 在 ES mapping 中增加 `category` 字段（`keyword` 类型）。
- `category` 过滤在 ES 查询层完成，避免拉回大量 ID 后再在 MySQL 过滤。
- 建议 `category` 也加入 MySQL 索引，用于不传 `search` 时的纯 MySQL 查询。

### 5. 返回字段带上 `category`

`GET /posts/:id` 和 `GET /posts/idListByAuthor/:author` 等返回帖子的接口，在 JSON 中增加：

```json
{
  "id": 100,
  "title": "...",
  "content": "...",
  "category": "问答",
  "created_at": "...",
  "update_at": "..."
}
```

前端 `Post` 模型会同步加上 `category` 字段。

### 6. （建议）分页参数

当前 `idList` 返回全部 ID，帖子一多会有性能问题。建议新增可选分页参数：

- `offset` / `limit` 或 `page` / `pageSize`
- 默认行为保持兼容（不传则返回全部），但建议文档中说明推荐传分页。

## 三、前后端约定

| 字段 | 类型 | 说明 |
|---|---|---|
| `search` | string | ES 全文检索关键词 |
| `author` | string | 精确匹配作者名 |
| `dateStart` | string | 开始日期，建议 ISO 8601 或 `yyyy-MM-dd` |
| `dateEnd` | string | 结束日期，建议 ISO 8601 或 `yyyy-MM-dd` |
| `category` | string | 品类枚举：`问答` / `资料` / `兴趣` / `梗图` |

## 四、前端当前兼容策略

- 作者、时间段会立即接入 `author`、`dateStart`、`dateEnd`。
- 品类选择 UI 先上线；在 `category` 参数未支持前，前端先用关键词在结果里二次过滤，等后端支持后切换为传 `category` 参数。
